set search_path [list . ./tech/STcmos65]

set libraries [list \
    CORE65LPLVT_nom_1.20V_25C.db \
]

set target_library [list]
lappend target_library [lindex $libraries 0]

set link_library [list *]
foreach library $libraries {
    lappend link_library $library
}